// let randomNumber = randomInteger(1, 1000);

// let numberUser = prompt('Введите число').replace(/ /g, "");

// window.start();
let startGame = "undefined";
if (startGame === "undefined") {
    startGame = window.start();
  (!startGame)? alert ('Игра остановлена'): alert ('Игра продолжается')
}
// goBot(numberUser);


